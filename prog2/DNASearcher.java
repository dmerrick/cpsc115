/*
 - dnaString: String // The string to search
 + DNASearcher(String sequence) // Constructor initializes the dnaString
 + readFile(String filename):String // Your are given this method which reads a file and returns its text
 + getRepeats(String substr): String // Returns all repeats of substr
 + getReverseRepeats(String substr):String // Returns first occurrence of substr and all its reverse repeats
 + getMirrorRepeats(String substr):String // Returns first occurrence of substr and all its DNA-mirror repeats
 + getPalindromes():String // Returns all palindromes of length 2 or more in dnaString
 + getNearPalindromes(int N):String // Returns all substrings where last N letters are the reverse of the first N.
 */

public class DNASearcher {
  
  //instance variables
  private String dnaString; // The string to search
  
  // Constructor initializes the dnaString
  public DNASearcher(String sequence) {
  }
  
  // Your are given this method which reads a file and returns its text
  public String readFile(String filename) {
    return "";
  }
  
  // Returns all repeats of substr
  public String getRepeats(String substr) {
    return "";
  }
  
  // Returns first occurrence of substr and all its reverse repeats
  public String getReverseRepeats(String substr) {
    return "";
  }
  
  // Returns first occurrence of substr and all its DNA-mirror repeats
  public String getMirrorRepeats(String substr) {
    return "";
  }
  
  // Returns all palindromes of length 2 or more in dnaString
  public String getPalindromes() {
    return "";
  }
  
  // Returns all substrings where last N letters are the reverse of the first N.
  public String getNearPalindromes(int N) {
    return "";
  }
  
}
